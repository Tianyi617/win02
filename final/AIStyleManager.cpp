/*#include "AIStyleManager.h"
#include <QDebug>

// 如果要用到 OpenCV / DNN 等，就在這裡包含
#include <opencv2/imgproc.hpp>
#include <opencv2/highgui.hpp>
#include <opencv2/dnn.hpp>
bool AIStyleManager::applyAIStyle(QPixmap &pixmap)
{
    // 這裡示範最簡單的範例：直接 debug 輸出
    // 真正 AI 風格處理，你可以把 pixmap 轉成 cv::Mat, 做推論, 再轉回 QPixmap
    qDebug() << "[AIStyleManager] applyAIStyle is called.";

    // TODO: 加入你的 AI 處理流程，若成功就 return true
    return true;
}*/
